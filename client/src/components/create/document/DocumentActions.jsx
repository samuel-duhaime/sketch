import styled from "styled-components";

const DocumentActions = () => {
   return <DocumentActionsContainer>DocumentActions</DocumentActionsContainer>;
};

const DocumentActionsContainer = styled.section`
   display: flex;
   width: 550px;
`;

export default DocumentActions;
